// This component is not used directly — GitHub stats are rendered
// inline in src/app/github-activities/page.js
// Keeping this file to avoid broken imports if referenced elsewhere.

export default function GithubStats({ stats }) {
  if (!stats) return null
  return null
}